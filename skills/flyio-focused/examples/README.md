# Examples

Use these examples for concrete snippets. Keep `SKILL.md` concise and reference examples instead of duplicating them.

## Available

- `minimal-web/`: minimal `fly.toml` + deploy command
- `deploy-strategies/`: rolling vs immediate command patterns
